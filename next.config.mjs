/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/jubilant-parakeet',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
